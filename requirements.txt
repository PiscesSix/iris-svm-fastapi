# Phụ thuộc lúc chạy API (dùng cho image Docker trên VPS).
# Phiên bản scikit-learn PHẢI trùng với lúc huấn luyện, nếu không file .pkl sẽ cảnh báo hoặc lỗi.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
scikit-learn==1.9.1
numpy==2.5.3
joblib==1.6.0
