__pycache__/
*.py[cod]
.venv/
.DS_Store

# LƯU Ý: KHÔNG bỏ qua svm_model.pkl — Render cần tệp này lúc khởi động.

# Hình cho báo cáo LaTeX — chỉ giữ ở máy, tái tạo bằng: python figures.py
figures/

# Cấu hình bí mật và CSDL cục bộ — tạo từ .env.example, seed lại bằng: python seed.py
.env
data/*.db
data/*.db-journal
